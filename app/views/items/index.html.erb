
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <!-- TITRE -->
    <title> CATTO
    </title>
    <!-- Important pour medias queries -->
    <meta content="initial-scale=1.0, width=device-width" name="viewport">
    <!-- Police de Caractère -->
    <link href="https://fonts.googleapis.com/css?family=Lobster" rel="stylesheet">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  	<link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
  	<link href="https://fonts.googleapis.com/css?family=Quicksand" rel="stylesheet">
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/font-awesome/4.6.0/css/font-awesome.min.css">
    <!-- FEUILLES DE STYLE -->
    <link rel="stylesheet" href="assets/css/styles.css">
    <!-- scrollmagic -->
    <script src="//cdnjs.cloudflare.com/ajax/libs/ScrollMagic/2.0.6/ScrollMagic.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/ScrollMagic/2.0.6/plugins/debug.addIndicators.min.js"></script>
<body>
    <header>
        <h1>Because Cat is Awesome</h1>
         <h2>WELCOME TO CATTO</h2> 
    </header>
    <section>
        <div class="img1">
            <div class="box box1">
    </section>
    <section>
        <div class="img2">
            <div class="box box2">
    </section>

    <section>
        <div class="img3">
            <div class="box box3">
    </section>
    <script src="app.js"></script>
</body>

</html>
<h1>Bienvenue dans Catto</h1>
<%if user_signed_in?%>
<strong><%= link_to "Ajouter un nouveau Cat_pic", new_item_path %></strong><br>

  <!-- find if currend_user has a cart else it create a cart for the user -->
	<% cart = Cart.find_by(user_id:current_user.id)
		if !cart
			Cart.create(user_id:current_user.id)
		end	
	%>
	<a href="<%=cart_path(Cart.find_by(user_id:current_user.id ))%>">mon panier</a>
<%end%>
     <% @items.each do |item| %>
	     <p>Titre: "<%= item.title %>"</p> 
	     <p>Description: "<%= item.description %>"</p> 
	     <p>Prix: "<%= item.price %> $"</p> 
	     <% if item.pic.attached? == true%>
	     <img class="a" <%= image_tag item.pic, alt: 'pic' %>
	     <br>
	     <%end%>

	     <button> <%= link_to "afficher", item_path(item.id)%> </button>
	     <%if user_signed_in?%>
	     <button id='<%=item.id%>' class="add">ajout a mon panier</button>
	     <%end%>
	     <h3>=========================</h3>
     <%end%>
